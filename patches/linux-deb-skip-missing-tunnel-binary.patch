diff --git a/build/linux/dependencies-generator.ts b/build/linux/dependencies-generator.ts
index 2fcb15e0..67e7d48a 100644
--- a/build/linux/dependencies-generator.ts
+++ b/build/linux/dependencies-generator.ts
@@ -3,6 +3,7 @@
  *  Licensed under the MIT License. See License.txt in the project root for license information.
  *--------------------------------------------------------------------------------------------*/
 import { spawnSync } from 'child_process';
+import { existsSync } from 'fs';
 import path from 'path';
 import { getChromiumSysroot, getVSCodeSysroot } from './debian/install-sysroot.ts';
 import { generatePackageDeps as generatePackageDepsDebian } from './debian/calculate-deps.ts';
@@ -56,8 +57,16 @@ export async function getDependencies(packageType: 'deb' | 'rpm', buildDir: stri
 	const appPath = path.join(buildDir, applicationName);
 	// Add the native modules
 	const files = findResult.stdout.toString().trimEnd().split('\n');
-	// Add the tunnel binary.
-	files.push(path.join(buildDir, 'bin', product.tunnelApplicationName));
+	// Add the tunnel binary, if this build produced one. This fork's CI
+	// doesn't build the Rust `code-tunnel` CLI (a separate Cargo pipeline
+	// Microsoft's official build runs, unrelated to the Node/gulp packaging
+	// done here), so the path never exists — scanning a nonexistent binary
+	// isn't just a soft warning like the native-modules case above, it's a
+	// hard crash inside dpkg-shlibdeps itself, so it must be skipped outright.
+	const tunnelBinaryPath = path.join(buildDir, 'bin', product.tunnelApplicationName);
+	if (existsSync(tunnelBinaryPath)) {
+		files.push(tunnelBinaryPath);
+	}
 	// Add the main executable.
 	files.push(appPath);
 	// Add chrome sandbox and crashpad handler.
